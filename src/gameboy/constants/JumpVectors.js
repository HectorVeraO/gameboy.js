/** RST Instruction symbol, just in case I need to differentiate */
export const RST_INSTRUCTION = Symbol('RST instruction');

/** Interrupt symbol, just in case I need to differentiate */
export const INTERRUPT = Symbol('Interrupt');
